<% sb.sub_budget_notations.each do |pbn| %>
	<tr data-id="<%= sbn.id %>">
	  <th scope="row"><%= pbn.title %></th>
	  <td class="hidden-sm hidden-xs""><%= pbn.description %></td>
	  <td><%= pbn.amount %></td>
	  <td><%= pbn.id %></td>
	  <td>
	  	<%#= link_to "Edit", edit_sub_budget_notation_path(id: pbn.id), remote: true, class: "btn btn-primary btn-xs", "data-toggle" => "modal", "data-target" => "#myModal" %>
			<%#= link_to "Delete", sub_budget_notation_path(id: pbn.id), remote: true, class: "btn btn-danger btn-xs",method: :delete %>
	  </td>
	</tr>
<% end %>
