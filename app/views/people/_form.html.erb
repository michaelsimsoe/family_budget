<%= form_for @person do |f| %>
	<%= f.label :name %>
	<%= f.text_field :name %>
	<%= f.label :description %>
	<%= f.text_area :description %>
	<%= f.label :disposable_amount %>
	<%= f.text_area :disposable_amount %>
	<%= f.submit %>
<% end %>