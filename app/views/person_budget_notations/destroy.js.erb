var ID = '<%= j @person_budget_notation.id.to_json %>';
var current = $("table").find(`tr[data-id=${ID}]`);

current[0].remove();

$.notify({
    icon: 'pe-7s-trash',
    message: "Notation has been deleted."

  },{
      type: 'danger',
      timer: 1000
});