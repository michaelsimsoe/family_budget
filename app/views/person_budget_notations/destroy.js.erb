var ID = '<%= j @person_budget_notation.id.to_json %>';
var current = $("table").find(`tr[data-id=${ID}]`);
var flash = createFlashMessage('<%= j flash[:notice] %>', '<%= j flash[:alert] %>');
$.notify({
    icon: 'pe-7s-trash',
    message: flash.message

  },{
      type: flash.messageType,
      timer: 500
});

current[0].remove();
