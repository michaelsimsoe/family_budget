$("#myModal").find('.modal-body').html("<%= j render 'form', person_budget_notation: @person_budget_notation %>");
$("#myModal").modal('show');

$.notify({
    icon: 'pe-7s-flag',
    message: "Notation has been updated."

  },{
      type: 'success',
      timer: 1000
});