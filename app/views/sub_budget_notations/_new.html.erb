<%= form_for @new_sub_budget_notation,  class: "form-inline", remote: true do |f| %>
	<div class="form-inline">
		<div class="form-group">
			<%= f.label :title, class: "sr-only" %>
			<%= f.text_field :title, class: "form-control", placeholder: "Title" %>

			<%= f.label :description, class: "sr-only" %>
			<%= f.text_field :description, class: "form-control", placeholder: "Description" %>

			<%= f.label :amount, class: "sr-only" %>
			<%= f.number_field :amount, class: "form-control", placeholder: "Amount" %>

			<%= f.label :notation_type, class: "sr-only" %>
			<%= f.text_field :notation_type, class: "form-control", placeholder: "Type" %>
		</div>
		<div class="form-group">
			<%= f.hidden_field :sub_budget_id, :value => sb.id %>
			<%= f.submit "Add", class: "btn btn-primary btn" %>
		</div>
	</div>
	</div>
<% end %>