$("#myModal").find('.modal-body').html("<%= j render 'form', sub_budget_notation: @sub_budget_notation %>");
$("#myModal").modal('show');

var flash = createFlashMessage('<%= j flash[:notice] %>', '<%= j flash[:alert] %>');

$.notify({
    icon: 'pe-7s-flag',
    message: flash.message

  },{
      type: flash.messageType,
      timer: 500
});
